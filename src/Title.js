import React, { Component } from 'react';

class Title extends Component {
    render() {
        return (
            <h1 id="app-title">Random Name Picker</h1>
        );
    }
}

export default Title;
